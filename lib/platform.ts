export function platformEntryUrl() {
  const value = process.env.NEXT_PUBLIC_PLATFORM_URL?.trim();
  if (!value) return null;
  try {
    const url = new URL(value);
    if (url.protocol !== "https:" && url.protocol !== "http:") return null;
    return url.toString();
  } catch {
    return null;
  }
}
